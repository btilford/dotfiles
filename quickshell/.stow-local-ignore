# Not stowed into $HOME
CLAUDE\.md
# Runtime wallust output — never stow/commit
wallust/colors\.json

# Repo meta — not stowed into $HOME. A package-local .stow-local-ignore
# REPLACES stow's built-in list (which covers README.*), it does not add to it.
README.*
