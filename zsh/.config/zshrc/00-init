# -----------------------------------------------------
# INIT
# -----------------------------------------------------

# -----------------------------------------------------
# Exports
# -----------------------------------------------------
export XDG_CONFIG_HOME="$HOME/.config"
export OS_KERN="$(uname -s)"
export EDITOR=nvim
export YARN_ENABLE_SCRIPTS=false
export PATH="/usr/lib/ccache/bin/:$PATH"

# -----------------------------------------------------
# Screenshot archives (machine-local)
# -----------------------------------------------------
# $VOL_SCREENSHOT_ARCHIVE / $DOTFILES_SCREENSHOT_ARCHIVE point into a notes vault
# that only exists on some hosts, so they are not tracked here — set them in
# ~/.zshrc_local. See scripts/visuals/README.md.
