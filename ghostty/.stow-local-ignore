# Not stowed into $HOME
CLAUDE\.md
# Runtime wallust output — never stow/commit. wallust writes the real file at
# ~/.config/ghostty/wallust.conf on every wallpaper switch; the repo's copy was
# an empty 0-byte placeholder, so stowing it would have blanked the palette.
wallust\.conf

# Repo meta — not stowed into $HOME. A package-local .stow-local-ignore
# REPLACES stow's built-in list (which covers README.*), it does not add to it.
README.*
