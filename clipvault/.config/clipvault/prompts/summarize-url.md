Fetch the URL below and summarize the page in a few short sentences.

Use WebFetch to retrieve it. If the fetch fails (unreachable, auth-walled,
paywalled, or not HTML), say so in one line and stop — do not guess at the
contents from the URL alone.

Output only the summary itself — no preamble, no headings, no code fences.
The output goes straight into clipboard history as-is.
