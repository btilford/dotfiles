Summarize the content below in a few short sentences.

If the content is a file path, read the file first — then summarize what you
found. (URLs are handled by the separate summarize-url prompt, which runs on a
fetch-capable harness; this one has no network tools.)

Output only the summary itself — no preamble, no headings, no code fences.
The output goes straight into clipboard history as-is.
